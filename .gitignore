# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies — unanchored so this also covers examples/*/node_modules
node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js — unanchored so this also covers examples/*/.next
.next/
/out/

# production
/build

# plugin build output
dist/
assets/

# jscpd duplication report (M9 Task 2) — CI-generated, appended to the job
# summary, not committed
.jscpd/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (but keep committed .env.example templates)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# superpowers brainstorm scratch
.superpowers/
